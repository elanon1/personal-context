---
type: project
project: Hexbane
area: plans
status: done
created: 2026-09-17
updated: 2026-09-17
verified: 2026-09-17
---
# Linear HEX-30–37 implementation

Authoritative scope: live Linear queue read 2026-09-17, all eight non-completed issues, no pagination remaining.
User requested implementation of all tasks. Preserve pre-existing client changes. No commits, pushes, deployment or credential configuration. Documentation remains in vault.

## Deliverables and acceptance
- [x] HEX-31: remove fake friends/requests/profile metadata; online from Nakama; empty lists remain empty and server failures do not simulate successful actions. Files: SocialScreen.cs, FriendDto.cs. Verify empty data, real offline mapping and service failure.
- [x] HEX-34: creation +/- adjusts one point, existing budget/floor checks remain. File: CreateCharacterScreen.cs. Verify boundary and ordinary clicks.
- [x] HEX-33: Discord and Buy Me a Coffee buttons use exact issue URLs, touch-sized controls and compact layout. File: DashboardScreen.cs. Verify rendered menu.
- [x] HEX-35: progression guidance is compact, bounded in width, visible on phones without covering target controls. Files: TutorialOverlay.cs, CharacterDetailScreen.Tutorial.cs if needed. Verify rendered progression tutorial.
- [x] HEX-36: passive_regeneration events do not create floating health numbers or passive-heal log entries; spell heals including 1 HP remain visible. Files: Player.cs, DuelProtocol.cs. Verify event filtering and unchanged HP snapshots.
- [x] HEX-37: single user-facing combat history; remove technical center status, retain actionable reconnect/error/queue controls, use readable authoritative events. Files: ReferenceHud.cs, ReferenceHud.DuelV2.cs. Verify live-shaped events and mobile layout.
- [x] HEX-32: account can own up to five characters; creation/select authorized by server and serialized, blocked while matching/in duel; all character reads/progression follow selected identity; one character auto-enters, multiple show selection with portrait/name/race/level; change-character replaces logout; tutorial is account-scoped. Server character module/migration and client auth/selection flow. Verify limits, ownership, concurrent requests, compatibility, selected-character isolation, tutorial and navigation.
- [x] HEX-30: real friend presence + in-match status; server-owned invitations with friendship/availability/expiry/replay checks; in-app invitation accept/decline leads to authorized duel; mobile notifications through actual platform integration. Verify two users, rejection, duplicate accept, expired invite and minimized devices. Push configuration availability is an open external dependency asked of user.

## Coordination
HEX-32 may change character/auth files and one dashboard logout binding. Other client corrections avoid those code regions. HEX-30 must use selected-character resolution from HEX-32, existing match admission/leases and existing notifications. Server currently clean; client has pre-existing changes recorded in session tool output. Initial client patch compiles: 0 errors, 11 existing warnings. No task marked complete yet.

## Outcome (second session, 2026-09-17)
The first session ran out of tokens after writing the code and a few notes, leaving no Linear comments and no journal entry. The second session verified everything from the uncommitted trees and closed all eight issues in Linear with per-ticket comments.

Verification evidence: client `dotnet build` 0 errors / 11 pre-existing warnings; `Scripts/check_aot_json.sh` OK; headless `Dev/BacklogClientVerification` (HEX-33/34, three viewports), `Dev/HitFeedbackVerification` (HEX-36/37, 33 checks), `Dev/CharacterDevelopmentVerification` and `Dev/PrimaryProgressionVerification` exit 0. Server `go vet ./...` and `go test ./...` green; `TestCharacterSelectionIsolationLimitAndAdmission` against a disposable `hexbane_test` database on the local Postgres; `make dev` rebuilt the Linux plugin, applied migrations 12–13 to the local stack and registered the seven new RPCs; HTTP scenarios (`server:scripts/e2e_invitations.py`) covered multi-character create/list/select/ownership/lock and the whole invitation state machine (see [[social]], [[character-selection]]).

Still open, deliberately: nothing committed or deployed (migrations 12–13 pending on production), push credentials and the Android AAR / iOS xcframework builds, device-rendered checks of the selection screen, the inbox dialog and the compact progression tooltip, and a two-client lobby join after an accepted invitation.
